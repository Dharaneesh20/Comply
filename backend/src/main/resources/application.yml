spring:
  application:
    name: align-compliance-backend
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:dev}
  data:
    mongodb:
      uri: ${MONGO_URI:mongodb://${MONGO_USERNAME:align_user}:${MONGO_PASSWORD:align_password}@${MONGO_HOST:localhost}:${MONGO_PORT:27017}/${MONGO_DATABASE:align_db}?authSource=admin}

jwt:
  secret: ${JWT_SECRET:align_compliance_platform_jwt_secret_key_production_grade_super_secure_32bytes_minimum_length!}
  expiration-ms: ${JWT_EXPIRATION_MS:86400000} # 24 hours

server:
  port: ${PORT:8081}
  error:
    include-message: always
    include-binding-errors: always

springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html
    operationsSorter: method

logging:
  level:
    root: INFO
    com.align.compliance: DEBUG
    org.springframework.data.mongodb: INFO
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n"
